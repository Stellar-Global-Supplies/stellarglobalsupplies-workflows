{
    "name": "postgres-forwarder",
    "version": "1.0.0",
    "description": "Cloudflare Worker — Supabase + NeonDB pg_stat_* metrics → New Relic",
    "private": true,
    "type": "module",
    "scripts": {
      "dev":    "wrangler dev",
      "deploy": "wrangler deploy",
      "tail":   "wrangler tail"
    },
    "dependencies": {
      "pg": "^8.12.0"
    },
    "devDependencies": {
      "wrangler": "^4.0.0"
    }
  }